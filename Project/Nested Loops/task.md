Nested loops are loops inside another loop. They are useful for 2D structures, patterns, grids, or combinations of items.

A classic example is a multiplication table, where one loop represents rows and another loop represents columns. Nested loops allow you to efficiently generate all combinations without repetition.

Example: Print a multiplication table from 1 to 5.